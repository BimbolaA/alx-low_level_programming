#include "holberton.h"

/**
 *_islower - check for lowercase characters
 *Return: return 1 for lowercase and 0 otherwise
 *@c: the character to print
 */
int _islower(int c)
{
return (c >= 97 && c <= 122);
}
